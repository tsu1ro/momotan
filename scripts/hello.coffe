cronJob = require('cron').CronJob

module.exports = (robot) ->
  cronjob = new cronJob(
    cronTime: "2 * * * * *"
    start:  true
    timeZone: "Asia/Tokyo"
    onTick: ->
      robot.send{room: "#momotan"}, "おはようございます"
  )